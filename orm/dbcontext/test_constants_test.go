package dbcontext

// Shared literals for tests to satisfy goconst within this package
const (
	tNameAlice = "Alice"
	tNameBob   = "Bob"
	tColName   = "name"
	tErrNilDB  = "database connection is nil"
)
